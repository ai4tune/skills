interface:
  display_name: "公众号发布助手"
  short_description: "将文章推送到公众号后台（默认草稿）"
  default_prompt: "Use $wechat-publish to push a WeChat article to the official account backend. Default mode is draft; direct publishing requires explicit confirmation."
