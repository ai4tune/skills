# Claude Code Instructions

Use this repository folder as a local business research skill.

Read `SKILL.md` for the operating procedure. The primary command is:

```bash
node scripts/run.mjs --input examples/tianjin-flooring.input.json --dry-run
```

For live generation, require `OPENAI_API_KEY`. Optional gateway variables are `OPENAI_BASE_URL` and `OPENAI_MODEL`.
